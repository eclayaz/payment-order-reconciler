{
  "name": "payment-order-reconciler-dev",
  "private": true,
  "description": "Local Docker dev/test environment for the payment-order-reconciler plugin. Not the plugin itself — that's in ./payment-order-reconciler.",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:logs": "wp-env logs",
    "env:cli": "wp-env run cli --",
    "env:setup": "bash ./bin/wp-env-setup.sh"
  },
  "devDependencies": {
    "@wordpress/env": "^10.0.0"
  }
}
